apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: {{ .Values.namespaces.app }}
spec:
  selector:
    app: mysql

  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: 3306

  type: ClusterIP