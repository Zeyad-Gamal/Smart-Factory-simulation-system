apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Values.namespaces.app }}
spec:
  selector:
    app: frontend

  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80

  type: {{ .Values.frontend.service.type }}