apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: {{ .Values.namespaces.app }}
spec:
  selector:
    app: backend

  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 8000

  type: {{ .Values.backend.service.type }}